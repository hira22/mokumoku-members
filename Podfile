# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'mokumoku-members' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  inhibit_all_warnings!

  # Pods for mokumoku-members
  pod 'Firebase/Analytics'
  pod 'Firebase/Auth'
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
   
  pod 'Ballcap'
  
  pod 'SwiftLint'

  target 'mokumoku-membersTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'mokumoku-membersUITests' do
    # Pods for testing
  end

end
